export const browser_intro_1 = {
  id: "browser_intro_1",
  title: "Что такое браузер и зачем он нужен",
  content: `
Браузер — это программа, которая позволяет пользователю **просматривать и взаимодействовать с веб-страницами**. Это ключевое звено между человеком и интернетом.

Когда вы вводите адрес сайта (например, \`https://example.com\`) в адресной строке, именно браузер отвечает за то, чтобы:
- найти нужный сервер,
- получить с него содержимое,
- отобразить веб-страницу на экране.

---

### Основные функции браузера

-  **Отправка HTTP/HTTPS-запросов** к удалённым серверам  
-  **Парсинг HTML, CSS, JavaScript** и преобразование в визуальный интерфейс  
-  **Взаимодействие с пользователем** через формы, кнопки, ввод и другие элементы  
-  **Обеспечение безопасности**: защита от XSS, HTTPS, политика одного источника (CORS) и др.  
-  **Кэширование и хранение данных** — локальное сохранение информации (cookies, localStorage, кеш)

### Популярные браузеры

- Google Chrome  
- Mozilla Firefox  
- Safari  
- Microsoft Edge  
- Opera  
- Яндекс.Браузер

Все они могут работать по-разному, но в основе лежит единый принцип обработки и отображения веб-контента.

---

### Зачем браузер frontend-разработчику?

Frontend-разработчик **создаёт интерфейсы**, которые именно браузер будет интерпретировать и отображать. Поэтому понимание того, как работает браузер, важно для:
- оптимизации загрузки и рендеринга,
- правильной работы JavaScript-кода,
- отладки и повышения производительности,
- обеспечения кроссбраузерной совместимости.

---

### Архитектура браузера (вкратце)

Современные браузеры включают в себя:
- **User Interface** (UI) — панель вкладок, адресная строка и т.п.  
- **Browser Engine** — координация между UI и рендерингом  
- **Rendering Engine** — преобразование HTML/CSS в визуальный интерфейс  
- **Networking** — сетевой стек для загрузки ресурсов  
- **JavaScript Engine** — интерпретация и выполнение JS (например, V8, SpiderMonkey)  
- **Data Storage** — куки, localStorage, IndexedDB и др.

---

### Вывод

Браузер — это **универсальный клиент**, который превращает код в интерфейс. Для frontend-разработчика это главный "исполнитель", а знание его принципов работы — залог стабильных и быстрых приложений.
  `.trim(),

  codeExample: `
<!-- Простой HTML-файл, который откроет браузер -->
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <title>Пример страницы</title>
  </head>
  <body>
    <h1>Привет из браузера!</h1>
    <p>Эту страницу отобразил ваш браузер.</p>
  </body>
</html>
  `.trim(),
};

export const browser_intro_2 = {
  id: "browser_intro_2",
  title: "Как работает интернет: основы сетей и протоколов",
  content: `
Интернет — это **глобальная сеть компьютеров**, которые могут связываться между собой. Когда вы открываете сайт, ваш компьютер общается с другим компьютером (сервером), и всё это происходит благодаря **сетям и протоколам**.

Представьте, что интернет — это **почтовая система**:
- Компьютеры — как дома или офисы с адресами.
- Данные — как письма и посылки.
- Протоколы — как правила доставки и форматирования писем.

---

### Как устроен путь от вас до сайта

Когда вы открываете сайт, например, \`https://example.com\`:
1. **DNS** находит IP-адрес сайта (как телефонная книга находит номер по имени).
2. Ваш браузер подключается к серверу по IP-адресу.
3. Браузер отправляет **HTTP-запрос**: "Привет, отправь мне главную страницу!"
4. Сервер отвечает: "Вот HTML-файл, CSS и JavaScript."
5. Браузер всё это получает, обрабатывает и отображает как веб-страницу.

---

### Основные протоколы

- **IP (Internet Protocol)** — доставка данных по адресу (как у каждой квартиры есть номер).
- **TCP (Transmission Control Protocol)** — надёжная доставка: гарантирует, что всё дойдёт и в нужном порядке.
- **HTTP/HTTPS** — общение между браузером и сервером (запросы и ответы).
- **DNS (Domain Name System)** — переводит имя сайта (example.com) в IP-адрес.

---

### Простой пример: вы открыли сайт

\`\`\`
Вы: Хочу открыть https://example.com

1. Ваш компьютер спрашивает у DNS: "Где живёт example.com?"
2. DNS отвечает: "Вот IP — 93.184.216.34"
3. Браузер подключается к этому IP и говорит: "GET /"
4. Сервер отвечает: "Вот тебе HTML-страница!"
5. Браузер показывает вам сайт.
\`\`\`

---

### Почему это важно frontend-разработчику?

- Знание **сетей** помогает понять, почему сайт может долго загружаться.
- Знание **HTTP** помогает писать правильные запросы (fetch, axios).
- Понимание **DNS, TCP, HTTPS** полезно при работе с API, авторизацией, SSL-сертификатами и CDN.

---

### Аналогия: интернет как доставка пиццы

- **Вы** — пользователь.
- **Звонок в пиццерию** — DNS-запрос.
- **Курьер** — IP-протокол.
- **Доставка заказа** — TCP.
- **Меню и заказ** — HTTP.

Если что-то не работает — нужно понимать, на каком этапе "курьер потерялся".

---

### Вывод

Интернет — это система, где миллионы компьютеров общаются между собой. Протоколы — это язык, на котором они говорят. Чтобы уверенно создавать веб-приложения, frontend-разработчику важно понимать, **как данные проходят от сервера до браузера**.
  `.trim(),
};

export const browser_intro_3 = {
  id: "browser_intro_3",
  title: "HTTP и HTTPS — основы протоколов",
  content: `
### Что такое HTTP?

**HTTP (HyperText Transfer Protocol)** — это протокол, по которому браузер и сервер **обмениваются данными**.  
Когда вы открываете сайт, браузер делает HTTP-запрос, а сервер отвечает HTTP-ответом.

 HTTP = "язык общения" между клиентом (браузером) и сервером.

Пример:
- Браузер: "GET /index.html"
- Сервер: "Вот тебе HTML-файл с кодом страницы"

---

### Как выглядит HTTP-запрос?

HTTP-запрос состоит из:
- метода (GET, POST и др.)
- пути (например, \`/products\`)
- заголовков (headers)
- тела запроса (body — при необходимости)

Пример запроса:
\`\`\`
GET /about HTTP/1.1
Host: example.com
User-Agent: Chrome
Accept: text/html
\`\`\`

А вот как сервер может ответить:
\`\`\`
HTTP/1.1 200 OK
Content-Type: text/html

<html>...</html>
\`\`\`

---

### Основные методы HTTP

- **GET** — получить данные (например, открыть страницу или список товаров)
- **POST** — отправить данные (например, форму регистрации)
- **PUT/PATCH** — изменить данные
- **DELETE** — удалить

---

### Что такое HTTPS?

**HTTPS (HTTP Secure)** — это тот же HTTP, **но с шифрованием**.  
Когда вы видите замок 🔒 в адресной строке — это HTTPS.

Для шифрования используется:
- Протокол **TLS** (раньше SSL)
- Специальный **сертификат**, выданный сайту

Зачем нужен HTTPS:
-  Защищает от перехвата данных (например, логина и пароля)
-  Гарантирует, что вы подключены к настоящему сайту, а не подделке

### Сравнение HTTP и HTTPS

| Протокол | Защита | Где используется |
|----------|--------|------------------|
| HTTP     |  Нет | Старые сайты, тесты |
| HTTPS    |  Да  | Все современные сайты |

Важно: большинство браузеров **блокируют** небезопасные HTTP-сайты, особенно при работе с формами.

---

### Почему это важно frontend-разработчику?

- Надо понимать, **как работают запросы** (особенно fetch, axios, формы и API)
- Нужно правильно указывать **методы, заголовки и тело запроса**
- Надо всегда использовать **HTTPS в продакшене**
- При разработке с backend важно знать, **что делает каждый метод** (GET vs POST и т.д.)

---

### Пример на практике

Когда вы отправляете форму на регистрацию:
1. Браузер формирует HTTP-запрос методом POST
2. Сервер принимает данные и отвечает
3. Вы получаете результат (успех или ошибка)

Если это всё происходит по HTTPS, ваши данные **защищены** от подглядывания.

---

### Вывод

HTTP и HTTPS — это основа всей работы в интернете.  
Frontend-разработчик постоянно использует эти протоколы, даже не замечая этого.  
Понимание разницы между ними помогает **писать безопасный и надёжный код**, особенно при работе с API и пользовательскими данными.
  `.trim(),
};

export const browser_intro_4 = {
  id: "browser_intro_4",
  title: "Основные компоненты браузера",
  content: `
Браузер — это сложная программа, состоящая из **нескольких компонентов**, которые работают вместе, чтобы загрузить и отобразить веб-страницу.

Давайте разберёмся, из чего он состоит.

### Основные части браузера

1. **User Interface (UI)** — это всё, что видит пользователь:
   - адресная строка,
   - кнопки "назад", "вперёд", "обновить",
   - вкладки и меню.

2. **Browser Engine** — отвечает за связь между UI и рендерингом страницы.  
   Он управляет тем, **когда и как запускать обработку страницы**.

3. **Rendering Engine** — обрабатывает HTML, CSS и превращает их в визуальный интерфейс.  
   Примеры: **Blink** (в Chrome), **WebKit** (в Safari), **Gecko** (в Firefox).

4. **Networking** — отвечает за сетевые запросы (загрузка HTML, CSS, JS, изображений и т.д.) через HTTP/HTTPS.

5. **JavaScript Engine** — исполняет JavaScript-код на странице.  
   Примеры: **V8** (Chrome), **SpiderMonkey** (Firefox), **JavaScriptCore** (Safari).

6. **Data Storage** — локальное хранение данных:
   - cookies
   - localStorage
   - sessionStorage
   - IndexedDB
   - Cache API

7.  **UI Backend** — отрисовка базовых элементов интерфейса, таких как шрифты и окна.

---

### Схема работы

\`\`\`
HTML + CSS + JS --> Rendering Engine --> Экран
Запросы --> Networking --> Получение ресурсов
JS --> JavaScript Engine --> Обработка логики
\`\`\`

---

### Почему это важно frontend-разработчику?

- Понимание, **в какой последовательности работает браузер**, помогает оптимизировать загрузку.
- Знание, как работает **рендеринг**, позволяет избежать лишних перерисовок (reflow, repaint).
- Умение использовать **storage API** позволяет хранить данные без сервера.

---

### Вывод

Современный браузер — это мощный механизм, объединяющий множество компонентов.  
Frontend-разработчик должен понимать его структуру, чтобы создавать **быстрые, безопасные и стабильные приложения**.
  `.trim(),
};

export const browser_intro_5 = {
  id: "browser_intro_5",
  title: "Популярные браузеры и движки",
  content: `
На рынке существует множество браузеров, но большинство из них используют **один из нескольких движков (rendering engines)**.  
Движок отвечает за то, **как именно будет отрисована веб-страница**.

### Популярные браузеры

| Браузер          | Движок         | Платформа        |
|------------------|----------------|------------------|
| **Google Chrome** | Blink (V8)     | Windows, macOS, Android |
| **Firefox**       | Gecko (SpiderMonkey) | Windows, macOS, Linux |
| **Safari**        | WebKit         | macOS, iOS       |
| **Edge**          | Blink (V8)     | Windows, macOS   |
| **Opera**         | Blink (V8)     | Windows, Android |
| **Яндекс.Браузер**| Blink (V8)     | Windows, Android |

>  Большинство современных браузеров основаны на **движке Blink**, разработанном Google.

### Основные движки рендеринга

- **Blink** — используется в Chrome, Edge, Opera, Яндекс.Браузере
- **WebKit** — родной движок Safari
- **Gecko** — движок Mozilla Firefox
- **Presto** (устаревший) — ранее использовался в старом Opera

---

### JavaScript-движки

- **V8** — Chrome, Edge, Opera
- **SpiderMonkey** — Firefox
- **JavaScriptCore** — Safari

---

### Почему важно знать движки?

- Некоторые фичи работают по-разному (особенно в старых версиях браузеров)
- Кроссбраузерность — важная часть frontend-разработки
- Отладка и тестирование должны учитывать **движок**, особенно при использовании новых API

---

### Вывод

Хотя браузеров много, "под капотом" у них может быть один и тот же движок.  
Понимание, **на каком движке работает браузер**, помогает адаптировать код и обеспечивать совместимость для всех пользователей.
  `.trim(),
};

export const browser_intro_6 = {
  id: "browser_intro_6",
  title: "Итоги: как работает браузер и зачем это знать",
  content: `
### Что такое браузер?

Браузер — это программа, которая **открывает сайты**.  
Он соединяется с интернетом, получает данные с серверов и **показывает веб-страницы** на экране.

---

### Как работает браузер шаг за шагом

1. Вы вводите адрес, например \`https://example.com\`.
2. Браузер спрашивает у DNS: "Где найти example.com?" — получает IP.
3. Отправляет **HTTP(S)-запрос** на сервер.
4. Получает ответ: HTML, CSS, JS.
5. Браузер **обрабатывает и рендерит** страницу на экране.

---

### Из чего состоит браузер?

- **UI** — кнопки, адресная строка, вкладки.
- **Browser Engine** — управляет процессом загрузки.
- **Rendering Engine** — превращает HTML и CSS в видимую страницу.
- **JavaScript Engine** — выполняет JS-код.
- **Networking** — загружает файлы.
- **Storage** — хранит данные: cookies, localStorage и др.

---

### Что такое HTTP и HTTPS?

- **HTTP** — простой протокол передачи данных.
- **HTTPS** — то же самое, но **с шифрованием** (защита данных).

Браузер использует их, чтобы **общаться с сервером**: отправлять и получать данные.

---

### Что важно знать frontend-разработчику?

- Как браузер **загружает и рендерит страницу** — это влияет на производительность.
- Как работает **сетевое общение** — HTTP/HTTPS, DNS.
- Как работает **JavaScript в браузере** — события, выполнение кода.
- Как браузер **хранит данные** — sessionStorage, localStorage, cookies.
- Как обеспечить **кроссбраузерную совместимость** — разные браузеры = разные движки.

---

### Простой пример

Открой DevTools в Chrome (F12) и посмотри:
- **Network** — какие запросы уходят (HTTP)
- **Elements** — как браузер построил DOM
- **Console** — что делает JavaScript

---

### Вывод

Браузер — это **главный инструмент** в руках frontend-разработчика.  
Чем лучше ты понимаешь его работу, тем быстрее и качественнее создаёшь сайты и приложения.  
Это основа, на которой строится весь frontend.

 **Понимание браузера = шаг к профессиональному уровню.**
  `.trim(),

  codeExample: `
<!-- Мини-пример: как браузер загружает страницу -->
<!DOCTYPE html>
<html>
  <head>
    <title>Пример</title>
    <script>
      console.log("Привет! Это JS, работает через движок V8");
    </script>
  </head>
  <body>
    <h1>Браузер отрисовал этот заголовок</h1>
  </body>
</html>
<!-- HTML + JS = страница, которую собрал и показал браузер -->
  `.trim(),
};
